# powershell字体更改

```
由于powershell字体选项中可选择字体极少，摸索出一个方法：
首先创建一个powershell的快捷方式，然后在快捷方式属性中将‘目标’项末尾追加一句：
-NoExit "chcp 65001"，用空格隔开。
其中数字 65001，也可以是437等。原因可以查询chcp命令的作用。

然后打开powershell查看字体选项就会有更多的字体选择了。
```

